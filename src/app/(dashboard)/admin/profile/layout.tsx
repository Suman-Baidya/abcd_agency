import React from "react";

export const metadata = {
  title: "Profile — ABCD Agency",
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
